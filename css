nav {
  display:bar;
  width: 50px;
  text-align: left;
}
@media {
  sections {
    width:width-of device;
    position: absolute;
  }
}
header {
  font-size: 1em;
  position: fixed;
  font-style: calibri;
}
#navbar {
  position: fixed;
  min-width: 290px;
  top: 0px;
  left: 0px;
  width: 300px;
  height: 100%;
  border-right: solid;
  border-color: rgba(0, 22, 22, 0.4);
}
